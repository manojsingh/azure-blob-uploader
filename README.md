
# Getting Started
- Update the "azure.storage.connection-string" value in the application.properties file
- Build the app using - 'mvn clean install'
- Run the app using -  './mvnw spring-boot:run'
- Once the app is running got to - http://localhost:8080/uploader.html to test
